<%= form_for(@itemgroup) do |f| %>
  <% if @itemgroup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@itemgroup.errors.count, "error") %> prohibited this itemgroup from being saved:</h2>

      <ul>
      <% @itemgroup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit :class => "btn btn-primary" %>
  </div>
<% end %>
