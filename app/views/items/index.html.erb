<p id="notice"><%= notice %></p>

<table class="table table-bordered">
  <thead>
  <tr>
    <th>Title</th>
    <th>Added by</th>
    <th colspan="2">Votes</th>
    <th></th>
  </tr>
  </thead>
  <tbody>

  <% @items.each do |item| %>
      <tr>
        <td><%= link_to item.name, item.url %></td>
        <td><%= item.user.email %></td>
        <td><%= item.plusminus %></td>
        <td><%= button_to('+1', vote_up_item_path(item), :class => "btn btn-mini btn-success", :form_class => "form-inline") %> <%= button_to('-1', vote_down_item_path(item), :class => "btn btn-mini btn-danger", :form_class => "form-inline") %></td>
        <td><%= link_to 'Show', item %> <%= link_to 'Edit', edit_item_path(item) %> <%= link_to 'Destroy', item, confirm: 'Are you sure?', method: :delete %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<br/>

<%= link_to 'New Item', new_item_path %>
